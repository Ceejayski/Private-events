
<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
  <%= link_to 'Private Events', root_path, class: "navbar-brand" %>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNavDropdown">
    <ul class="navbar-nav ml-auto">
      <% if logged_in? %>
        <li class="nav-item">
            <%= link_to "#{current_user.name}", user_path(current_user), class: "nav-link" %>
        </li>
        <li class="nav-item">
            <%= link_to 'Create Event', new_event_path(@event), class: "nav-link"%>
        </li>
        <!--<li class="nav-item">
            <%#= link_to 'Attended Event', event_path(event), class: "nav-link"%>
        </li>-->
        <li class="nav-item">
            <%= link_to 'Logout', logout_path(@user), class: "nav-link", method: :delete %>
        </li>
      <% else %>
        <li class="nav-item">
            <%= link_to 'Sign up', new_user_path(@user), class: "nav-link" %>
        </li>
      <%end%>
    </ul>
  </div>
</nav>
